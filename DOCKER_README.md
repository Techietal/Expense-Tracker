# Docker Setup - ExpenseTracker

This project is configured to run inside Docker using ASP.NET Core and SQLite.

## Run locally

From the same folder as `ExpenseTracker.csproj`, run:

```bash
docker compose up --build